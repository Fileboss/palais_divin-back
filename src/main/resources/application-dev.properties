# Local-dev defaults. Activated by `spring.profiles.default=dev` in application.properties.
# Prod ships with SPRING_PROFILES_ACTIVE=prod so this file is never loaded there.
app.keycloak.client-secret=test-backend-secret
app.invitation.signup-base-url=http://localhost:5173/register
app.minio.access-key=palaisdivin
app.minio.secret-key=palaisdivin
